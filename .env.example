## Contoh konfigurasi environment
# Path base saat deploy (GitHub Pages repo /ssoportal/ atau './' untuk root relatif)
VITE_BASE_PATH=/ssoportal/

# Master password development (hanya aktif ketika import.meta.env.DEV === true)
VITE_MASTER_PASSWORD=dev-master-2025

# Daftar email admin dipisah koma / spasi / titik koma
VITE_ADMIN_EMAILS=admin@local,mentor@example.com

# Google OAuth credentials (opsional)
VITE_GOOGLE_CLIENT_ID=your-google-client-id-here
VITE_GOOGLE_REDIRECT_URI=http://localhost:5173

# Base path untuk build produksi.
# Kosongkan atau gunakan './' untuk shared hosting di subfolder mana pun.
# Untuk GitHub Pages (repo bernama ssoportal):
# VITE_BASE_PATH=/ssoportal/
# VITE_BASE_PATH=./
